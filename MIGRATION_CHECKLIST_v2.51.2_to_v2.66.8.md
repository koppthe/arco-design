# Arco Design 升级迁移测试清单

## 版本：v2.51.2 → v2.66.8

本文档提供详细的测试清单和具体的测试用例，帮助你系统地验证升级后的应用。

---

## 一、升级前准备

### 1.1 代码审查

- [ ] **代码搜索：查找所有 Arco 组件使用**
  ```bash
  # 查找所有导入 Arco 组件的文件
  grep -r "from '@arco-design/web-react'" src/
  grep -r "import.*@arco-design" src/
  ```

- [ ] **样式搜索：查找所有样式覆盖**
  ```bash
  # 查找所有 .arco-* 类名的样式覆盖
  grep -r "\.arco-" src/ --include="*.css" --include="*.less" --include="*.scss"
  
  # 特别关注的组件样式
  grep -r "\.arco-carousel" src/
  grep -r "\.arco-avatar" src/
  grep -r "\.arco-btn-group" src/
  grep -r "\.arco-select" src/
  grep -r "\.arco-table" src/
  ```

- [ ] **Props 使用检查**
  ```bash
  # 检查 Cascader showSearch 使用
  grep -r "showSearch.*renderOption" src/
  
  # 检查 InputNumber onChange 使用
  grep -r "InputNumber.*onChange" src/
  
  # 检查 VirtualList onScroll 使用
  grep -r "VirtualList.*onScroll" src/
  ```

### 1.2 创建测试环境

- [ ] 创建新的 Git 分支
  ```bash
  git checkout -b upgrade/arco-design-v2.66.8
  ```

- [ ] 备份 package.json 和 yarn.lock/package-lock.json
  ```bash
  cp package.json package.json.backup
  cp yarn.lock yarn.lock.backup  # 或 cp package-lock.json package-lock.json.backup
  ```

### 1.3 依赖升级

- [ ] 更新 Arco Design 版本
  ```bash
  # 使用 npm
  npm install @arco-design/web-react@2.66.8
  
  # 使用 yarn
  yarn add @arco-design/web-react@2.66.8
  ```

- [ ] 清除缓存并重新安装
  ```bash
  # npm
  rm -rf node_modules package-lock.json
  npm install
  
  # yarn
  rm -rf node_modules yarn.lock
  yarn install
  ```

- [ ] 验证版本
  ```bash
  npm list @arco-design/web-react
  # 应该显示 @arco-design/web-react@2.66.8
  ```

---

## 二、编译和构建测试

### 2.1 TypeScript 编译

- [ ] **运行 TypeScript 编译**
  ```bash
  npm run tsc --noEmit
  # 或
  yarn tsc --noEmit
  ```

- [ ] **检查类型错误**
  - 特别关注 Cascader、InputNumber、VirtualList 的类型错误
  - 检查新组件（ColorPicker、VerificationCode、Watermark）的导入

- [ ] **修复类型错误（如有）**
  ```typescript
  // 示例：修复 Cascader renderOption 类型
  showSearch={{
    renderOption: (
      inputValue: string,
      option: NodeProps<T>,
      extraOptions?: { checked: boolean }  // 添加新参数类型
    ) => {
      // ...
    }
  }}
  ```

### 2.2 项目构建

- [ ] **开发环境构建**
  ```bash
  npm run dev
  # 或
  yarn dev
  ```

- [ ] **检查构建日志**
  - 查找警告和错误
  - 特别注意样式相关的警告

- [ ] **生产环境构建**
  ```bash
  npm run build
  # 或
  yarn build
  ```

- [ ] **检查构建产物大小**
  ```bash
  # 比较升级前后的 bundle 大小
  ls -lh dist/
  ```

---

## 三、样式回归测试

### 3.1 高风险组件样式测试

#### Carousel（轮播图）

**测试场景 1：卡片模式 - 水平**
- [ ] 页面路径：_______________________
- [ ] 测试步骤：
  1. 打开包含 Carousel 卡片模式的页面
  2. 检查初始显示是否正常
  3. 点击切换到下一张，检查动画是否流畅
  4. 检查前一张、当前、下一张的透明度和位置
  5. 快速连续切换，检查是否有卡顿或错位
- [ ] 预期结果：动画流畅，卡片位置正确
- [ ] 实际结果：_______________________
- [ ] 截图/视频：_______________________

**测试场景 2：卡片模式 - 垂直（新功能）**
- [ ] 测试代码：
  ```tsx
  <Carousel
    animation="card"
    direction="vertical"
    style={{ height: 400 }}
  >
    <div>卡片 1</div>
    <div>卡片 2</div>
    <div>卡片 3</div>
  </Carousel>
  ```
- [ ] 检查垂直卡片切换动画
- [ ] 检查卡片的 3D 透视效果
- [ ] 实际结果：_______________________

**测试场景 3：指示器位置 - outer-right（新功能）**
- [ ] 测试代码：
  ```tsx
  <Carousel
    indicatorPosition="outer-right"
    style={{ height: 300 }}
  >
    {/* 内容 */}
  </Carousel>
  ```
- [ ] 检查指示器是否显示在右侧
- [ ] 检查指示器的旋转效果
- [ ] 实际结果：_______________________

#### Avatar（头像）

**测试场景：头像布局对齐**
- [ ] 页面路径：_______________________
- [ ] 测试场景：
  1. 文本与头像在同一行
  2. 头像在 flex 容器中
  3. 头像组（AvatarGroup）
  4. 头像与其他组件组合
- [ ] 检查点：
  - [ ] 头像的垂直对齐是否正确
  - [ ] 头像与文本的基线对齐
  - [ ] 在不同容器中的表现
- [ ] 如有问题，应用临时修复：
  ```css
  .arco-avatar-image {
    display: inline-block !important;
  }
  ```
- [ ] 实际结果：_______________________

#### Button（按钮）

**测试场景：按钮组中的单个按钮**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <Button.Group>
    <Button>单个按钮</Button>
  </Button.Group>
  ```
- [ ] 检查单个按钮的圆角是否正确显示
- [ ] 检查不同尺寸的按钮组
- [ ] 检查 round 形状的按钮组
- [ ] 实际结果：_______________________

#### Select（选择器）

**测试场景：大尺寸字体**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <Select size="large" options={[...]} />
  ```
- [ ] 检查字体大小是否为 14px
- [ ] 检查视觉效果是否协调
- [ ] 如有问题，应用样式修复：
  ```css
  .arco-select-size-large .arco-select-view-value {
    font-size: 16px; /* 或其他期望的大小 */
  }
  ```
- [ ] 实际结果：_______________________

#### Table（表格）

**测试场景 1：mini 尺寸**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <ConfigProvider size="mini">
    <Table columns={columns} data={data} />
  </ConfigProvider>
  ```
- [ ] 检查表格是否正确继承 mini 尺寸
- [ ] 实际结果：_______________________

**测试场景 2：RTL 模式（如果项目使用）**
- [ ] 测试代码：
  ```tsx
  <ConfigProvider rtl>
    <Table columns={columns} data={data} />
  </ConfigProvider>
  ```
- [ ] 检查表格从右到左的显示
- [ ] 检查固定列的位置
- [ ] 实际结果：_______________________

### 3.2 中等风险组件样式测试

#### Input（输入框）

**测试场景：清除按钮在 Safari**
- [ ] 浏览器：Safari
- [ ] 测试步骤：
  1. 输入文本
  2. 点击清除按钮
  3. 观察是否有垂直抖动
- [ ] 实际结果：_______________________

#### Tabs（标签页）

**测试场景：线型标签页的墨水条**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <Tabs type="line">
    <Tabs.TabPane key="1" title="Tab 1" />
    <Tabs.TabPane key="2" title="Tab 2" />
  </Tabs>
  ```
- [ ] 检查墨水条的显示和动画
- [ ] 实际结果：_______________________

---

## 四、功能测试

### 4.1 Form（表单）

**测试场景 1：表单验证**
- [ ] 页面路径：_______________________
- [ ] 测试步骤：
  1. 提交空表单，检查验证提示
  2. 填写部分字段，检查部分验证
  3. 填写完整表单，检查提交成功
- [ ] 检查点：
  - [ ] 验证规则是否正常工作
  - [ ] 错误提示是否正确显示
  - [ ] 表单状态是否正确
- [ ] 实际结果：_______________________

**测试场景 2：字段依赖（dependencies）**
- [ ] 页面路径：_______________________
- [ ] 测试代码示例：
  ```tsx
  <Form>
    <Form.Item name="country" label="国家">
      <Select />
    </Form.Item>
    <Form.Item 
      name="city" 
      label="城市"
      dependencies={['country']}
    >
      <Select />
    </Form.Item>
  </Form>
  ```
- [ ] 测试步骤：
  1. 选择国家
  2. 检查城市选项是否更新
  3. 更改国家
  4. 检查城市选项是否重新加载
- [ ] 实际结果：_______________________

**测试场景 3：validateOnly（新功能）**
- [ ] 测试代码：
  ```tsx
  const [form] = Form.useForm();
  
  const handleValidate = async () => {
    try {
      await form.validate(undefined, { validateOnly: true });
      console.log('验证通过，但不触发状态更新');
    } catch (error) {
      console.log('验证失败');
    }
  };
  ```
- [ ] 检查验证是否执行但不触发 UI 更新
- [ ] 实际结果：_______________________

### 4.2 Table（表格）

**测试场景 1：排序功能**
- [ ] 页面路径：_______________________
- [ ] 测试数据：包含 undefined 值的数据集
  ```tsx
  const data = [
    { id: 1, name: 'Alice', age: 25 },
    { id: 2, name: 'Bob', age: undefined },
    { id: 3, name: 'Charlie', age: 30 },
    { id: 4, name: 'David', age: null },
  ];
  ```
- [ ] 测试步骤：
  1. 点击年龄列排序
  2. 检查 undefined 和 null 值的排序位置
  3. 切换升序/降序
- [ ] 实际结果：_______________________

**测试场景 2：虚拟滚动**
- [ ] 页面路径：_______________________
- [ ] 测试数据：大量数据（1000+ 行）
- [ ] 测试步骤：
  1. 滚动表格
  2. 检查滚动流畅度
  3. 检查数据加载是否正常
  4. 快速滚动，检查是否有闪烁
- [ ] 实际结果：_______________________

**测试场景 3：展开行和树形数据**
- [ ] 测试步骤：
  1. 展开/收起行
  2. 检查展开动画
  3. 检查树形数据的层级显示
- [ ] 实际结果：_______________________

### 4.3 Select（选择器）

**测试场景 1：maxTagCount（新功能）**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <Select
    mode="multiple"
    maxTagCount={3}
    options={[
      { label: '选项1', value: '1' },
      { label: '选项2', value: '2' },
      { label: '选项3', value: '3' },
      { label: '选项4', value: '4' },
      { label: '选项5', value: '5' },
    ]}
  />
  ```
- [ ] 测试步骤：
  1. 选择超过 3 个选项
  2. 检查是否显示 "+N" 标签
  3. 悬停在 "+N" 标签上，检查弹出框
- [ ] 实际结果：_______________________

**测试场景 2：allowCreate**
- [ ] 页面路径：_______________________
- [ ] 测试步骤：
  1. 输入自定义选项
  2. 回车创建
  3. 检查是否添加成功
  4. 将 value 设为 undefined，检查是否报错
- [ ] 实际结果：_______________________

### 4.4 Cascader（级联选择器）

**测试场景 1：搜索功能**
- [ ] 页面路径：_______________________
- [ ] 测试步骤：
  1. 输入搜索关键字
  2. 检查搜索结果
  3. 选择搜索结果中的选项
  4. 检查选中状态是否正确显示（checked）
- [ ] 实际结果：_______________________

**测试场景 2：自定义图标（新功能）**
- [ ] 测试代码：
  ```tsx
  import { IconLoading, IconCheck, IconRight } from '@arco-design/web-react/icon';
  
  <Cascader
    icons={{
      loading: <IconLoading />,
      checked: <IconCheck />,
      next: <IconRight />,
    }}
    options={options}
  />
  ```
- [ ] 检查自定义图标是否显示
- [ ] 实际结果：_______________________

### 4.5 DatePicker（日期选择器）

**测试场景 1：inputProps（新功能）**
- [ ] 测试代码：
  ```tsx
  <DatePicker
    inputProps={{
      placeholder: '请选择日期',
      onFocus: () => console.log('聚焦'),
      onBlur: () => console.log('失焦'),
    }}
  />
  ```
- [ ] 测试步骤：
  1. 检查占位符
  2. 聚焦输入框，检查回调
  3. 失焦，检查回调
- [ ] 实际结果：_______________________

**测试场景 2：RangePicker fixedTime（新功能）**
- [ ] 测试代码：
  ```tsx
  <RangePicker fixedTime />
  ```
- [ ] 测试步骤：
  1. 选择日期范围
  2. 检查时间是否固定
- [ ] 实际结果：_______________________

### 4.6 Input（输入框）

**测试场景：suffix 和 addAfter 同时使用**
- [ ] 页面路径：_______________________
- [ ] 测试代码：
  ```tsx
  <Input
    suffix={<IconSearch />}
    addAfter="搜索"
  />
  ```
- [ ] 检查是否同时显示 suffix 和 addAfter
- [ ] 实际结果：_______________________

### 4.7 新组件测试

#### ColorPicker（颜色选择器）

**测试场景 1：基础功能**
- [ ] 测试代码：
  ```tsx
  const [color, setColor] = useState('#165DFF');
  
  <ColorPicker
    value={color}
    onChange={setColor}
    showText
  />
  ```
- [ ] 测试步骤：
  1. 点击打开颜色面板
  2. 选择颜色
  3. 检查颜色值是否更新
  4. 检查颜色文本显示
- [ ] 实际结果：_______________________

**测试场景 2：渐变色模式**
- [ ] 测试代码：
  ```tsx
  <ColorPicker
    mode={['single', 'gradient']}
    defaultValue={[
      { color: '#165DFF', percent: 0 },
      { color: '#722ED1', percent: 100 },
    ]}
  />
  ```
- [ ] 测试步骤：
  1. 切换到渐变色模式
  2. 添加/删除色标
  3. 调整色标位置
  4. 修改色标颜色
- [ ] 实际结果：_______________________

#### VerificationCode（验证码输入框）

**测试场景：验证码输入**
- [ ] 测试代码：
  ```tsx
  <VerificationCode
    length={6}
    onChange={(value) => console.log(value)}
  />
  ```
- [ ] 测试步骤：
  1. 输入验证码
  2. 检查自动聚焦到下一个输入框
  3. 删除验证码，检查焦点回退
  4. 粘贴验证码，检查自动填充
- [ ] 实际结果：_______________________

#### Watermark（水印）

**测试场景：页面水印**
- [ ] 测试代码：
  ```tsx
  <Watermark content="机密文档">
    <div style={{ height: 500 }}>
      {/* 页面内容 */}
    </div>
  </Watermark>
  ```
- [ ] 测试步骤：
  1. 检查水印是否显示
  2. 尝试通过开发者工具删除水印
  3. 检查水印是否自动恢复（防删除机制）
- [ ] 实际结果：_______________________

---

## 五、浏览器兼容性测试

### 5.1 桌面浏览器

**Chrome**
- [ ] 版本：_______________________
- [ ] 测试结果：
  - [ ] 所有页面正常显示
  - [ ] 交互功能正常
  - [ ] 样式无异常
- [ ] 问题记录：_______________________

**Firefox**
- [ ] 版本：_______________________
- [ ] 测试结果：
  - [ ] 所有页面正常显示
  - [ ] 交互功能正常
  - [ ] 样式无异常
- [ ] 问题记录：_______________________

**Safari**
- [ ] 版本：_______________________
- [ ] 重点测试：Input 清除按钮
- [ ] 测试结果：
  - [ ] Input 清除时无抖动
  - [ ] 其他功能正常
- [ ] 问题记录：_______________________

**Edge**
- [ ] 版本：_______________________
- [ ] 测试结果：
  - [ ] 所有页面正常显示
  - [ ] 交互功能正常
  - [ ] 样式无异常
- [ ] 问题记录：_______________________

### 5.2 移动浏览器

**iOS Safari**
- [ ] 版本：_______________________
- [ ] 测试结果：_______________________

**Android Chrome**
- [ ] 版本：_______________________
- [ ] 测试结果：_______________________

---

## 六、性能测试

### 6.1 页面加载性能

**首页加载**
- [ ] 升级前加载时间：_______ ms
- [ ] 升级后加载时间：_______ ms
- [ ] 变化：_______ ms (↑/↓ _____%)

**包体积对比**
```bash
# 记录升级前后的 bundle 大小
```
- [ ] 升级前主包大小：_______ KB
- [ ] 升级后主包大小：_______ KB
- [ ] 变化：_______ KB (↑/↓ _____%)

### 6.2 运行时性能

**Table 虚拟滚动**
- [ ] 测试数据量：_______ 行
- [ ] 滚动帧率：
  - 升级前：_______ FPS
  - 升级后：_______ FPS
- [ ] 内存使用：
  - 升级前：_______ MB
  - 升级后：_______ MB

**复杂表单渲染**
- [ ] 表单字段数量：_______
- [ ] 首次渲染时间：
  - 升级前：_______ ms
  - 升级后：_______ ms

---

## 七、集成测试

### 7.1 路由跳转测试

- [ ] 从页面 A 跳转到页面 B
- [ ] 检查组件状态保持
- [ ] 检查样式正确加载

### 7.2 状态管理测试

- [ ] Redux/Zustand 集成正常
- [ ] 组件状态更新正常
- [ ] 无异常报错

### 7.3 API 调用测试

- [ ] Form 提交正常
- [ ] Table 数据加载正常
- [ ] Select 远程搜索正常

---

## 八、自动化测试

### 8.1 单元测试

```bash
npm run test
# 或
yarn test
```

- [ ] 所有测试用例通过
- [ ] 无新增失败用例
- [ ] 测试覆盖率：_______

### 8.2 端到端测试

```bash
npm run e2e
# 或
yarn e2e
```

- [ ] 关键流程测试通过
- [ ] 无回归问题

---

## 九、问题记录和解决方案

### 问题 1
- **描述**：_______________________
- **组件**：_______________________
- **重现步骤**：_______________________
- **解决方案**：_______________________
- **状态**：□ 未解决 □ 已解决 □ 待确认

### 问题 2
- **描述**：_______________________
- **组件**：_______________________
- **重现步骤**：_______________________
- **解决方案**：_______________________
- **状态**：□ 未解决 □ 已解决 □ 待确认

### 问题 3
- **描述**：_______________________
- **组件**：_______________________
- **重现步骤**：_______________________
- **解决方案**：_______________________
- **状态**：□ 未解决 □ 已解决 □ 待确认

---

## 十、最终检查

### 10.1 代码质量

- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 警告
- [ ] 无 console.log/console.error
- [ ] 代码已格式化

### 10.2 文档更新

- [ ] 更新项目依赖文档
- [ ] 更新开发指南（如有新组件使用）
- [ ] 更新 CHANGELOG

### 10.3 团队沟通

- [ ] 已通知团队成员升级计划
- [ ] 已分享测试结果
- [ ] 已同步破坏性变更
- [ ] 已准备回滚方案

---

## 十一、上线计划

### 11.1 发布策略

- [ ] **灰度发布**
  - 阶段 1：5% 用户，观察 _____ 小时
  - 阶段 2：20% 用户，观察 _____ 小时
  - 阶段 3：50% 用户，观察 _____ 小时
  - 阶段 4：100% 用户

- [ ] **监控指标**
  - 错误率：目标 < _____
  - 页面加载时间：目标 < _____ ms
  - API 响应时间：目标 < _____ ms

### 11.2 回滚准备

- [ ] 备份当前生产版本
- [ ] 准备回滚脚本
- [ ] 测试回滚流程
- [ ] 确定回滚决策人

---

## 十二、升级完成确认

- [ ] 所有测试通过
- [ ] 所有问题已解决或有workaround
- [ ] 性能符合预期
- [ ] 团队已准备就绪
- [ ] 监控已配置
- [ ] 文档已更新

**升级负责人签名**：_______________________  
**日期**：_______________________

**评审人签名**：_______________________  
**日期**：_______________________

---

**文档版本**: 1.0  
**更新日期**: 2026-01-09  
**适用版本**: Arco Design v2.51.2 → v2.66.8
